typedef struct Product {
	// Is this correct?
	char* name;
	unsigned int code;
	// Pointless
	// unsigned int quantity;
	// float price;
	// float max_dicount_rate;
} product_t;

// Also need to si plify the struct
// typedef struct Product product_t;

// typedef struct product_t product_array[];
// TODO: Check if floats can have bit fields
